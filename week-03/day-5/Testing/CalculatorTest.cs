using System;
using System.Collections.Generic;
using System.Text;

namespace Testing
{
    class CalculatorTest
    {
        /*
        [Fact]
        public void AdditionTest()
        {
            // arrange create a new calculator
            var testingCalculator = new Calculator();

            // act call the calculator
            var actualOutput = testingCalculator.Add(2, 5);
            var expectedOutput = 7;

            // assert  - compares the output value from the test and the value we expect

            Assert.Equal(expectedOutput, actualOutput);
        }

        [Theory]
        [InlineData(-7, -2, -9)]
        [InlineData(2, 5, 7)]
        [InlineData(-2, 5, 7)]
        public void GenericAdditionTest(int firstNumber, int secondNumber, int expectedOutPut)
        {
            var testingCalculator = new Calculator();
            var actualOutput = testingCalculator.Add(firstNumber, secondNumber);
            var expectedOutput = expectedOutPut;

            Assert.Equal(expectedOutput, actualOutput);
        }
        */
    }
}
